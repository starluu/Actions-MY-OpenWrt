name: Docker 测试

on:
  workflow_dispatch:

jobs:
  alist-automation:
    runs-on: ubuntu-latest
    
    steps:
    # ========== 初始阶段：内网穿透 ==========
    - name: 使用 皎月连 穿透
      run: |
        docker run -d --name natpierce \
            --restart=always \
            --privileged \
            --net=host \
            -v natpierce_data:/natpierce \
            xiyu505/natpierce

    - name: 使用 cpolar 穿透
      run: |
        docker run -id --network host --name cpolar probezy/cpolar
        docker exec -i cpolar /bin/bash
        ./cpolar authtoken ${{ secrets.CPOLAR_TOKEN }}

    - name: 使用 Ngrok 穿透
      run: |
        docker run -id -e NGROK_AUTHTOKEN=${{ secrets.NGROK_TOKEN }} ngrok/ngrok:alpine http 33272

    # ========== 部署阶段：容器初始化 ==========
    - name: 启动 portainer 容器
      run: |
        docker run -d --restart=always --name="portainer" -p 9000:9000 -v /var/run/docker.sock:/var/run/docker.sock 6053537/portainer-ce:2.24.1-alpine
           echo "⏳ 等待手动配置测试项目"
           echo "👉 预留6小时手动操作时间"
        sleep 6h
